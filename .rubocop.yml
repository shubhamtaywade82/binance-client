# The behavior of RuboCop can be controlled via the .rubocop.yml
# configuration file. It makes it possible to enable/disable
# certain cops (checks) and to alter their behavior if they accept
# any parameters. The file can be placed either in your home
# directory or in some project directory.
#
# RuboCop will start looking for the configuration file in the directory
# where the inspected file is and continue its way up to the root directory.
#
# See https://docs.rubocop.org/rubocop/configuration

AllCops:
  NewCops: enable
  SuggestExtensions: false
  # Unified-SDK core work-in-progress (not yet lint-clean).
  Exclude:
    - lib/binance/core/rate_limit/**/*
    - lib/binance/core/transport/**/*
    - lib/binance/helpers/**/*
    # Generated by script/generate_catalog.rb — one dense hash literal per endpoint.
    - lib/binance/core/catalog.rb

Metrics/ParameterLists:
  Max: 6

# RSpec describe/it blocks are structured differently from production methods.
Metrics/BlockLength:
  Exclude:
    - spec/**/*

# Generic product client with catalog-driven request dispatch.
Metrics/ClassLength:
  Exclude:
    - lib/binance/products/api.rb

# Overridable callback stubs are single-line by design.
Style/EmptyMethod:
  EnforcedStyle: compact
